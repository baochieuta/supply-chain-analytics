# Dashboards

Place the Tableau workbook (`.twbx`) here, and export a static PNG of the main dashboard view
to `../reports/figures/dashboard_overview.png` so it renders directly in the main README
(Tableau Public links can go stale or require login — always embed a static image as backup).
